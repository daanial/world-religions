VITE_MAPBOX_TOKEN=pk.your_mapbox_public_token_here

# Server-only — used by scripts/generate-tts.ts (build) and /api/tts (dev/preview only)
# Production playback uses pre-generated public/audio/*.mp3; /api/tts is disabled there.
OPENAI_API_KEY=sk-your_openai_api_key_here
